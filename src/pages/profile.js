import React, { Fragment } from 'react';
import { ProfileDefault } from '../components/features';

const Profile = () => {
  return (
    <Fragment>
      <ProfileDefault />
    </Fragment>
  );
};

export default Profile;
